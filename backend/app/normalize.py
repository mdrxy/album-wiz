"""
Function to normalize an image to a square shape.
"""

from PIL import Image


def crop_to_square(image: Image):
    """
    Crop an image to a square shape.

    Parameters:
    - image (Image): The input image to crop.

    Returns:
    - Image: The cropped image.
    """
    pass
    changed_img = image.copy()
    return changed_img
